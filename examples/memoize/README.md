# Memoize Runtime Example

Showcases `memoizeRuntime` caching repeated calls.

```bash
bun run examples/memoize/src/index.ts
```

You should see the "computing" log only for unique keys within the TTL window.
