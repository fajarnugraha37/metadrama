# Trace Runtime Example

Wraps an async function with `traceRuntime` to emit start/resolve lifecycle messages.

```bash
bun run examples/trace/src/index.ts
```

Observe the structured logs showing args, duration, and results for each invocation.
