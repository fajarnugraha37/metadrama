# Pointcut Selector Example

Illustrates how class/method/function pointcuts evaluate against different signatures.

```bash
bun run examples/pointcut/src/index.ts
```

The script prints a table showing which pointcuts matched each mocked signature.
