# Validate Runtime Example

Uses TypeBox schemas with `validateRuntime` to enforce argument shapes.

```bash
bun run examples/validate/src/index.ts
```

The second call intentionally fails to show the validation error.
