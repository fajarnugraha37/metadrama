# Free Function Advice Example

Attaches before/after advice to a standalone function via `weaveFunction`.

```bash
bun run examples/freefn/src/index.ts
```

You should see log lines before and after the email payload is produced.
