# Examples

- `basic/` – shows before/after/around advice on a decorated class.
- `macros/` – demonstrates compile-time macros (memoize, retry, trace, validate).

Use `metadrama build <path>` to transform the example sources.
